name: Moto G45 Fogos-Specific SUSFS + KernelSU (5.4.274)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout Kernel Source
      uses: actions/checkout@v4
      with:
        repository: Motorola-SM6375-Devs/android_kernel_motorola_sm6375
        ref: sixteen
        fetch-depth: 1

    - name: Install Build Tools
      run: |
        sudo apt-get update
        sudo apt-get install -y bc bison build-essential flex git libssl-dev libelf-dev gcc-aarch64-linux-gnu device-tree-compiler make cpio python3

    - name: Integrate KernelSU
      run: |
        curl -LSs https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh | bash -s main

    - name: Integrate SUSFS
      run: |
        # Use the specific kernel-5.4 branch for SUSFS
        git clone https://gitlab.com/simonpunk/susfs4ksu.git -b kernel-5.4 --depth=1 susfs_src
        cp susfs_src/kernel_patches/fs/susfs.c fs/
        cp susfs_src/kernel_patches/include/linux/susfs.h include/linux/
        
        # Patch KernelSU to support SUSFS hiding
        if [ -d "KernelSU" ]; then
          cp susfs_src/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch KernelSU/
          cd KernelSU && patch -p1 < 10_enable_susfs_for_ksu.patch && cd ..
        fi
        
        # Patch the main kernel for SUSFS
        patch -p1 < susfs_src/kernel_patches/50_add_susfs_in_kernel-5.4.patch || true

    - name: Configure for Moto G45 Fogos (All Fixes Included)
      run: |
        export ARCH=arm64
        
        # FIX 1: Force Kernel Sublevel to 274 (Fixes Wi-Fi/BT Initialization)
        sed -i 's/^SUBLEVEL = .*/SUBLEVEL = 274/' Makefile
        
        # FIX 2: Match your EXACT Stock version string (Ensures Stock Wi-Fi drivers load)
        # Your string: 5.4.274-moto-00517-g2ad8f9e38ea6
        sed -i 's/echo "+"/#echo "+"/' scripts/setlocalversion
        echo "-moto-00517-g2ad8f9e38ea6" > localversion
        
        # FIX 3: Use the fogos_defconfig found in your logs
        CONF_NAME="fogos_defconfig"
        
        # Generate initial .config using the vendor path shown in your logs
        make O=out ARCH=arm64 vendor/$CONF_NAME

        # FIX 4: Enable KernelSU and SUSFS
        ./scripts/config --file out/.config -e CONFIG_KERNELSU
        ./scripts/config --file out/.config -e CONFIG_KERNELSU_SUSFS
        ./scripts/config --file out/.config -e CONFIG_KERNELSU_SUSFS_HAS_MAGIC_MOUNT
        
        # FIX 5: Network & Wi-Fi Stability for Stock ROM
        ./scripts/config --file out/.config -e CONFIG_WLAN
        
        # FIX 6: Disable Module Signature Enforcement
        # Crucial because your new kernel won't have Motorola's private keys
        ./scripts/config --file out/.config -d CONFIG_MODULE_SIG
        ./scripts/config --file out/.config -d CONFIG_MODULE_SIG_ALL
        ./scripts/config --file out/.config -d CONFIG_MODULE_SIG_FORCE
        
        # Finalize configuration
        make O=out ARCH=arm64 olddefconfig

    - name: Build Kernel
      run: |
        export ARCH=arm64
        export CROSS_COMPILE=aarch64-linux-gnu-
        # Build both Image.gz-dtb and Image.gz to be safe
        make -j$(nproc) O=out ARCH=arm64 CC=aarch64-linux-gnu-gcc Image.gz-dtb || make -j$(nproc) O=out ARCH=arm64 CC=aarch64-linux-gnu-gcc Image.gz

    - name: Organize Artifacts
      run: |
        mkdir -p deploy
        find out/arch/arm64/boot/ -name "Image.gz*" -exec cp {} deploy/ \;
        find out/arch/arm64/boot/dts/vendor/qcom/ -name "*.dtbo" -exec cp {} deploy/ \; || echo "No DTBOs"

    - name: Upload Build
      uses: actions/upload-artifact@v4
      with:
        name: Moto-G45-Fogos-KSU-SUSFS-Final
        path: deploy/*
